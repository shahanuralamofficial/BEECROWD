#include <bits/stdc++.h>
using namespace std;
int main()
{
    int i, a, o = 0;
    cin >> a;
    for (i = 1; i <= a; i++)
    {
        if (i % 2 != 0)
        {
            cout << i << endl;
        }
    }

    return 0;
}