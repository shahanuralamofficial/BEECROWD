#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a;
    cin >> a;
    for(int i = 1; i < a*4;){
            cout << (i) << " " << (i+1) << " " << (i+2) << " PUM\n";
            i+=4;
    }
    return 0;
}
